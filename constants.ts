/**
 * The constants for this project are defined here
 */

export const PREFIX = {
  PROJECT: "my",
  PORPUSE: "settings",
};

export const RESOURCE_PREFIX = `${PREFIX.PROJECT}-${PREFIX.PORPUSE}`;

export const RESOURCE_NAME = {
  STACK: "stack",
  BUCKET: "bucket",
  KEY: "key",
  TRAIL: "trail",
  TOPIC: "topic",
  EVENTS_RULE: "event-rule",
};
